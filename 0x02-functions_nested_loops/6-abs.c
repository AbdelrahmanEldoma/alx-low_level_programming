#include <stdio.h>
#include <stdlib.h>
int _abs(int);
/**
*_abs - checks of if a number
*positive, negative or zero
*
*@n: a variable
*Return: returns absolute value of n
*/
int _abs(int n)
{
	return (abs(n));
}
